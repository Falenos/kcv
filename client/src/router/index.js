import dashboard from './routes/dashboard';
import login from './routes/login';
import cv from './routes/cv';
import editCV from './routes/editCV';

export const routes = [
    dashboard,
    login,
    cv,
    editCV
];

export default routes;
